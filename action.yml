# action.yml
name: 'Assign to Project'
description: 'Assign Issue or Pull Request to default project'
inputs:
  project:  # id of input
    description: 'Assigned Project'
    required: true
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.project }}
